# 산출물 — 압축된 배열을 펼치면 저장소가 커지고, 다음 파이프라인 실행이 다시 압축해 무한 churn이 된다
public/data/

# 빌드 입력 — universe.json은 스크립트가 형식을 관리하고, cache는 NDJSON이라 JSON 파서로 다룰 수 없다
data/

# 수집 스크립트는 한 문장을 폭 때문에 끊지 않는 스타일로 통일돼 있다 (주석 규칙과 같은 원칙)
scripts/
